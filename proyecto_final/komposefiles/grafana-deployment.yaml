apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: final
  annotations:
    kompose.cmd: /home/javier/Descargas/kompose convert -f docker-compose.yml -o komposefiles2/ --volumes hostPath
    kompose.version: 1.26.0 (40646f47)
  labels:
    io.kompose.service: grafana
  name: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: grafana
  template:
    metadata:
      annotations:
        kompose.cmd: /home/javier/Descargas/kompose convert -f docker-compose.yml -o komposefiles2/ --volumes hostPath
        kompose.version: 1.26.0 (40646f47)
      labels:
        io.kompose.service: grafana
    spec:
      containers:
        - env:
            - name: GF_SECURITY_ADMIN_PASSWORD
              value: admin
          image: javieresquivel123/proyecto-final-grafana:8277f559d8beb99084af869213bab317b209f2b9
          imagePullPolicy: Always
          name: grafana
          resources:
            requests:
              cpu: "500m"      # Reserva 0.25 CPUs
              memory: "100Mi"    # Reserva 64 Megabytes
            limits:
              cpu: "1"         # Máximo 1 CPU
              memory: "500Mi"
          ports:
            - containerPort: 3000
              protocol: TCP
      restartPolicy: Always
